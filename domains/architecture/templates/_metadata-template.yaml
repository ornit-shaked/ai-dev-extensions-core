# Architecture Intake Metadata: {{SERVICE_NAME}}

## Collection Summary
timestamp: {{TIMESTAMP}}
workflow_version: 1.0.0
project_name: {{SERVICE_NAME}}
project_type: {{microservice|library|ui-service}}

## Section Completeness

sections:
  identity:
    source: SEMI
    completeness: {{COMPLETE|PARTIAL|MISSING}}
    needs_human: {{true|false}}
    risk: {{LOW|MEDIUM|HIGH}}
    notes: "{{ANY_SPECIFIC_NOTES}}"
    
  architecture:
    source: AUTO
    completeness: {{COMPLETE|PARTIAL|MISSING}}
    needs_human: false
    risk: LOW
    notes: "{{ANY_SPECIFIC_NOTES}}"
    
  contracts:
    source: SEMI
    completeness: {{COMPLETE|PARTIAL|MISSING}}
    needs_human: {{true|false}}
    risk: {{HIGH|MEDIUM|LOW}}
    notes: "{{ANY_SPECIFIC_NOTES}}"
    
  data:
    source: AUTO
    completeness: {{COMPLETE|PARTIAL|MISSING}}
    needs_human: false
    risk: LOW
    notes: "{{ANY_SPECIFIC_NOTES}}"
    
  flows:
    source: SEMI
    completeness: {{COMPLETE|PARTIAL|MISSING}}
    needs_human: {{true|false}}
    risk: MEDIUM
    notes: "{{ANY_SPECIFIC_NOTES}}"
    
  errors:
    source: AUTO
    completeness: {{COMPLETE|PARTIAL|MISSING}}
    needs_human: false
    risk: LOW
    notes: "{{ANY_SPECIFIC_NOTES}}"
    
  config:
    source: AUTO
    completeness: {{COMPLETE|PARTIAL|MISSING}}
    needs_human: false
    risk: LOW
    notes: "{{ANY_SPECIFIC_NOTES}}"
    
  observability:
    source: SEMI
    completeness: {{COMPLETE|PARTIAL|MISSING}}
    needs_human: {{true|false}}
    risk: MEDIUM
    notes: "{{ANY_SPECIFIC_NOTES}}"

## Overall Statistics

total_sections: 8
complete_sections: {{COUNT}}
partial_sections: {{COUNT}}
missing_sections: {{COUNT}}
needs_human_review: {{COUNT}}

## Security Assessment

security_applicable: {{true|false}}
security_notes: |
  {{IF_SECURITY_FEATURES_DETECTED}}
  - Spring Security detected: {{true|false}}
  - OAuth2/JWT detected: {{true|false}}
  - Authentication endpoints: {{true|false}}
  
  {{IF_NOT_APPLICABLE}}
  Security section not applicable - backend service with no direct user authentication.

## ADR Status

adr_location: {{docs/adr/|MISSING}}
adr_count: {{COUNT_OR_0}}
adr_notes: |
  {{IF_FOUND}}
  ADRs found at: {{LOCATION}}
  
  {{IF_MISSING}}
  No ADRs found. Consider creating docs/adr/ directory.
  Template: https://github.com/joelparkerhenderson/architecture-decision-record

## Collection Issues

issues_count: {{COUNT}}
high_priority_issues: {{COUNT}}
medium_priority_issues: {{COUNT}}
low_priority_issues: {{COUNT}}

## Recommendations

next_steps:
  - "{{RECOMMENDATION_1}}"
  - "{{RECOMMENDATION_2}}"
  - "{{RECOMMENDATION_3}}"

validation_checklist:
  - "Review non-goals section (requires business context)"
  - "Verify OpenAPI spec location or create manual documentation"
  - "Add dashboard links to observability section"
  - "Review and approve key flows for completeness"
  - "{{OTHER_ITEMS}}"

## Agent Consumption Notes

agent_ready: {{true|false}}
agent_notes: |
  This architecture intake is {{READY|PARTIALLY_READY|NOT_READY}} for agent consumption.
  
  {{IF_READY}}
  All essential sections (identity, architecture, contracts, data, flows, config) are complete.
  Agents can safely use this information for bug fixes and feature implementation.
  
  {{IF_PARTIAL}}
  Some sections need human review. Agents should treat flagged items as uncertain.
  Review _open-issues.md for items requiring human input.
  
  {{IF_NOT_READY}}
  Critical sections are missing or incomplete. Human review required before agent use.

## File Manifest

files_generated:
  - 1-identity.md
  - 2-architecture.md
  - 3-contracts.md
  - 4-data.md
  - 5-flows.md
  - 6-errors.md
  - 7-config.md
  - 8-observability.md
  - _metadata.yaml (this file)
  - _open-issues.md

---
# Auto-generated by /architecture-intake workflow
